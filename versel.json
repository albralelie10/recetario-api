{    
    "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
    "headers": [
        {
        "Access-Control-Allow-Origin": "*",
        "Access-Control-Allow-Credentials" :"true"
        }
      
    ]
}
